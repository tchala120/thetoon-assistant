import styled from '@emotion/styled'

const PageTitle = styled.span`
  padding-top: 24px;
  display: block;
  font-size: 28px;
  font-weight: bold;
  color: #f8f8f8;
  margin-bottom: 32px;
`

export default PageTitle
